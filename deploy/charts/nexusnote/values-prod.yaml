# ===========================================
# NexusNote Helm Chart - Production Values
# ===========================================
# 仅覆盖与 values.yaml 不同的值

namespace: nexusnote

env:
  NODE_ENV: "production"
  NEXT_PUBLIC_APP_URL: "https://juanie.art"
  NEXTAUTH_URL: "https://juanie.art"

# Infisical 配置
infisical:
  enabled: true
  projectSlug: "nexusnote-ibqe"
  envSlug: "prod"
