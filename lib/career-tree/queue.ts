export async function enqueueCareerTreeCompose(_userId: string): Promise<void> {
  // Queue wiring lands in the next implementation slice.
  // Keep the async boundary now so API semantics are already correct.
}
